pipeline {
    agent { 
        label {
            label 'master'
            customWorkspace 'workspace/OCI_precon_publish/'
        }
    }
    stages {
        stage('build') {
            steps {
                sh 'pwd > workspace'
                workspace = readFile('workspace').trim()
                echo ${workspace}
            }
        }
    }
}
